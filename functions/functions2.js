
/* const myFunction = (x) => {
    if (typeof x == "undefined") {
        x = "GBP"
    }
    console.log(x);

}

myFunction(); 

const myFunction = (x = 'GBP') => {
    console.log(x);
}

myFunction();
*/

/* const myFunc = (currency='GBP', lang='en', whatever = 'sef') => {
    // if (typeof currency == 'undefined') {
    // currency = 'GBP';
    // }
    // if (typeof lang == 'undefined') {
    // currency = 'en';
    // }
    console.log(currency);
    console.log(lang);
    }

    myFunc('null', 'zh'); */

    /* const options = (darkMode='true', fontSize='18pt', highContrast='false', fontWeight='standard') => {

    }

    options(undefined, '22pt', undefined, 'bold'); // we pass in undefined when we don't want to affect th eparameter
*/
/*let myVar = 0;
console.log(myVar);
// js is run from top to bottom


const myFunc = (param1) => {
    console.log('hellos');
}

myFunc();

myOtherFunc();

function myOtherFunc(params) {
    console.log('hello');
}

*/
